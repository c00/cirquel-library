import { JbImageDirective } from './jb-image.directive';

describe('JbImageDirective', () => {
  it('should create an instance', () => {
    const directive = new JbImageDirective();
    expect(directive).toBeTruthy();
  });
});
